# SumoController

帮我编写一个可模块化的集控小工具。
- 项目名：SumoController
- 编写语言：暂不定，当前有Python、Node.js、JDK、.NET SDK等开发环境。但要求小巧，最终构建产物为exe可执行文件，无需额外安装运行库（最好别，除非没别的办法了）。如果是C#，那么模块可以以.cipx形式分发。
- 包含（若需要）：cn.craftime.sumocontroller（大小写自己定）

所需的组成部分：

### SumoController:
主程序，负责以管理员身份开机自启，以及启动别的模块。
运行时，若无管理员权限，则进行一次重启，重启时进行UAC提权。每次启动时都检查是否有在开机时以管理员身份运行主程序的系统计划任务。若无，创建之。
自动生成配置文件config.yml（或别的格式，按需即可），其中可配置集控配置主服务器（用于自动下载组件及其配置文件）。记得写好注释。
自动生成plugins目录，组件的文件、配置文件都在此处。

### AutoWLAN
一个自动打开WLAN并连接指定网络的组件。网络SSID、加密类型、密码在本组件配置文件中配置。一样，写好注释。

### AntiEdgeSurf
一个自动检测当前是否有标题为"edge://surf"（不区分大小写、正反斜杠）的窗口并自动关闭之的组件。

### BoxLogger
一个自动记录电脑上任何打开过的窗口的标题、时间、主程序路径以及其更改、关闭，自动上传到集控中心的组件。

### AntiBluetooth
自动关闭设备的蓝牙的组件。

### AntiLine
禁用设备的有线网络连接的组件。

### VerifyWLANMac
自动根据配置文件中的WLAN的SSID及其密码、MAC地址验证已连接的网络，若其中有任意MAC对不上，自动断开连接。一个SSID可有多个MAC。

除此以外，写一个服务端，不一定是exe可执行文件，因为我大概率全用Linux运行它。